# Core AI webhook endpoint - This is where you send a webhook to your remote ai service
AI_WEBHOOK_URL=https://your-ai-service.internal/webhooks/start

#This is the name of your custom model I.E externai-ai
#Example, you could do something like "Information Model for clientX"
MODEL_NAME=your-custom-model-name




# Optional authentication secret that will be sent as an Authorization header
# AI_API_KEY=

# Optional timeout in seconds (defaults to 30)
# AI_TIMEOUT=45

# Optional named webhook targets exposed to the trigger_webhook tool.
# EXTRA_WEBHOOKS={
#   "n8n_status": {
#     "url": "https://n8n.example.com/webhook/status",
#     "method": "POST",
#     "headers": {"x-api-key": "secret"},
#     "secret": "optional-shared-secret"
#   }
# }
