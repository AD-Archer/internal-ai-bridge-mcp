okay im having a bit of trouble so im gonna explain to you again. basically i want to use openweb ui connected to the mcp server /mcp/openai or whatever to send a webhook to our backend. which i call ninjacat its a in house ai server that can be activated via a webhook. after the backend is done. i want to send the message back to the frontend using a post webhook. i want to make sure the url. /callback works and can handle a webhook and send it back to this mcp so this mcp can give it to our frontend